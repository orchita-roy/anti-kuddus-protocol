"use client";import { useEffect,useState } from "react";import { AppShell } from "@/components/AppShell";import { apiRequest } from "@/lib/api/client";export default function Page(){const[state,setState]=useState<any>();useEffect(()=>{void apiRequest("/api/complaints/public-stats").then(setState)},[]);return <AppShell title="Three-strike accountability"><div className="eyebrow">Teacher-unlocked democratic review</div><h1>{state?.verifiedCount??"—"} verified warnings.</h1><div className="card"><h2>{state?.verifiedCount>=3?"Impeachment review is eligible":"Vote remains locked"}</h2><p>Default safeguards: three verified warnings, 60% turnout, 66% support, and a 24-hour voting period. Eligibility never opens a vote automatically, and results never remove an account or role automatically.</p></div></AppShell>}
