"use client";import { useCallback,useEffect,useState } from "react";import { useParams } from "next/navigation";import { AppShell } from "@/components/AppShell";import { apiRequest } from "@/lib/api/client";import { DemocracyStatusBadge,VotingPanel,TurnoutMeter,ConfidenceMeter,ElectionCountdown } from "@/components/democracy/DemocracyComponents";export default function Page(){const{id}=useParams<{id:string}>(),[item,setItem]=useState<any>(),[result,setResult]=useState<any>(),[error,setError]=useState("");const load=useCallback(()=>{void apiRequest<any>(`/api/democracy/proposals/${id}`).then(async x=>{setItem(x);if(x.poll)setResult(await apiRequest(`/api/democracy/polls/${x.poll._id}/results`))}).catch(e=>setError(e.message))},[id]);useEffect(load,[load]);return <AppShell title="Proposal"><div className="eyebrow">Evidence-based class governance</div>{error&&<div className="error">{error}</div>}{!item?<div className="card empty">Loading proposal…</div>:<><DemocracyStatusBadge status={item.status}/><h1>{item.title}</h1><p>{item.description}</p>{item.poll&&<div className="grid"><div style={{gridColumn:"span 6"}}><VotingPanel poll={item.poll} onVoted={load}/></div><div className="card" style={{gridColumn:"span 6"}}><h2>Safe aggregate audit</h2><ElectionCountdown endsAt={item.poll.votingEndsAt}/>{result&&<><TurnoutMeter value={result.turnoutPercent}/>{typeof result.approvalPercent==="number"&&<ConfidenceMeter value={result.approvalPercent}/>}</>}<p>No individual choice is exposed.</p></div></div>}</>}</AppShell>}
