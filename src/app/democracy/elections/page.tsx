"use client";import { useEffect,useState } from "react";import { AppShell } from "@/components/AppShell";import { apiRequest } from "@/lib/api/client";import { DemocracyStatusBadge,ElectionCountdown } from "@/components/democracy/DemocracyComponents";export default function Page(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{void apiRequest<any[]>("/api/democracy/elections").then(setItems)},[]);return <AppShell title="Captain elections"><div className="eyebrow">Anonymous candidate selection</div><h1>Leadership by consent.</h1><div className="grid">{items.length?items.map(x=><div className="card" style={{gridColumn:"span 6"}} key={x._id}><DemocracyStatusBadge status={x.status}/><h2 style={{marginTop:12}}>{x.title}</h2><p>{x.description}</p><ElectionCountdown endsAt={x.votingEndsAt}/></div>):<div className="card empty" style={{gridColumn:"span 12"}}>No elections scheduled.</div>}</div></AppShell>}
