import { AppShell } from "@/components/AppShell";import { ChatShell } from "@/components/assistant/ChatShell";export default function Page(){return <AppShell title="ClassGuard AI Copilot"><div className="eyebrow">Authorized sources only</div><h1>Ask with privacy intact.</h1><p>ClassGuard can explain stored rules, curriculum, public safety guidance, TruthLens results, and democratic workflows. It cannot inspect private identities or evidence.</p><ChatShell/></AppShell>}
