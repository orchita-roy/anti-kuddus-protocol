import Link from "next/link";import { AppShell } from "@/components/AppShell";import { Radio } from "lucide-react";
export default function CaptainDashboard(){return <AppShell title="Captain command center"><div className="eyebrow">Biltu–Miltu rescue network</div><h1>Respond fast.<br/>Protect the source.</h1><div className="card" style={{maxWidth:700}}><Radio size={32} color="var(--red)"/><h2>Anonymous alert queue</h2><p>Incoming alerts reveal only a public ID, location, time, and status. No sender identity is stored or broadcast.</p><Link className="btn primary" href="/captain/sos">Open live alert queue</Link></div></AppShell>}
