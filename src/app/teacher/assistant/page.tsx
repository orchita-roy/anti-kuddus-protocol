import { AppShell } from "@/components/AppShell";import { ChatShell } from "@/components/assistant/ChatShell";export default function Page(){return <AppShell title="Teacher ClassGuard"><div className="eyebrow">Sanitized operational intelligence</div><h1>Ask across aggregate signals.</h1><p>Teacher tools expose statistics and summaries—not student identities, raw complaint evidence, or individual voting choices.</p><ChatShell teacher/></AppShell>}
