"use client";import { useCallback,useEffect,useState } from "react";import { AppShell } from "@/components/AppShell";import { RumorTrendChart } from "@/components/truthlens/TruthLens";import { apiRequest } from "@/lib/api/client";export default function Page(){const[data,setData]=useState<any>(),[error,setError]=useState("");const load=useCallback(()=>void apiRequest<any>("/api/teacher/truthlens/analytics").then(setData).catch(e=>setError(e.message)),[]);useEffect(load,[load]);return <AppShell title="TruthLens intelligence"><div className="eyebrow">Anonymous aggregate analytics</div><h1>Clarify emerging claims.</h1>{error&&<div className="error">{error}</div>}{data?<div className="grid"><div style={{gridColumn:"span 7"}}><RumorTrendChart items={data.trend}/></div><div className="card" style={{gridColumn:"span 5"}}><h2>Frequent claims</h2>{data.frequentClaims.length?data.frequentClaims.map((x:any)=><p key={x._id}>{x._id} · {x.count}</p>):<div className="empty">No rumor clusters.</div>}<p>{data.privacy}</p></div></div>:<div className="card empty">Loading anonymous trends…</div>}</AppShell>}
