"use client";import { useEffect,useState } from "react";import { AppShell } from "@/components/AppShell";import { apiRequest } from "@/lib/api/client";
type Log={_id:string;action:string;entityType:string;safeMetadata:Record<string,unknown>;createdAt:string};
export default function Audit(){const [items,setItems]=useState<Log[]>([]);useEffect(()=>{apiRequest<Log[]>("/api/teacher/audit").then(setItems)},[]);return <AppShell title="Safe audit trail"><div className="eyebrow">Privileged actions only</div><h1>Accountability without identity leaks.</h1><div className="notice">This log intentionally excludes complaint submitters, roll numbers, secrets, API keys, sessions, and image metadata.</div><div className="card" style={{marginTop:16}}>{items.length?<table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Safe metadata</th></tr></thead><tbody>{items.map(x=><tr key={x._id}><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.action}</td><td>{x.entityType}</td><td>{JSON.stringify(x.safeMetadata)}</td></tr>)}</tbody></table>:<div className="empty">No privileged actions recorded yet.</div>}</div></AppShell>}
