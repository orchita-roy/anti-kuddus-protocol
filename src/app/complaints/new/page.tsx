"use client";import { FormEvent,useState } from "react";import { AppShell } from "@/components/AppShell";import { apiRequest } from "@/lib/api/client";import { ShieldCheck } from "lucide-react";
export default function ComplaintPage(){const [busy,setBusy]=useState(false),[error,setError]=useState(""),[receipt,setReceipt]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");try{const form=new FormData(e.currentTarget);form.set("confirmed","true");const data=await apiRequest<{publicId:string}>("/api/complaints",{method:"POST",body:form});setReceipt(data.publicId);e.currentTarget.reset()}catch(e){setError(e instanceof Error?e.message:"Submission failed")}finally{setBusy(false)}}return <AppShell title="Anonymous whistleblower"><div className="grid"><section style={{gridColumn:"span 7"}}><div className="eyebrow">Mission 01 · Secure intake</div><h1>Report abuse.<br/>Keep your identity.</h1><p>Your complaint record has no student, roll number, session, or IP field. A separate daily HMAC token limits abuse without linking you to a complaint.</p>{receipt?<div className="card"><ShieldCheck color="var(--green)"/><h2>Complaint received</h2><p>Save this anonymous receipt. It is the only identifier returned.</p><div style={{fontSize:28,fontWeight:900,letterSpacing:2}}>{receipt}</div><button className="btn" style={{marginTop:20}} onClick={()=>setReceipt("")}>Submit another</button></div>:<div className="card"><form onSubmit={submit}><div className="field"><label>Category</label><select name="category" required><option value="">Choose category</option><option value="tiffin_theft">Tiffin theft</option><option value="washroom_bribe">Washroom bribe</option><option value="syllabus_bloat">Syllabus bloat</option><option value="seat_abuse">Seat abuse</option><option value="sports_abuse">Sports abuse</option><option value="other">Other</option></select></div><div className="field"><label>Description</label><textarea name="description" minLength={20} maxLength={2000} required placeholder="Describe what happened, without naming yourself."/></div><div className="grid"><div className="field" style={{gridColumn:"span 6"}}><label>Incident date</label><input name="incidentDate" type="date" max={new Date().toISOString().slice(0,10)} required/></div><div className="field" style={{gridColumn:"span 6"}}><label>Location</label><input name="location" required maxLength={100}/></div></div><div className="field"><label>Photo evidence (optional)</label><input name="evidence" type="file" accept="image/jpeg,image/png,image/webp"/><small className="muted">JPEG, PNG, or WebP · max 5 MB · metadata stripped server-side</small></div><label className="notice"><input type="checkbox" required style={{width:"auto",marginRight:8}}/>I confirm this report is truthful to the best of my knowledge.</label>{error&&<div className="error">{error}</div>}<button className="btn primary" disabled={busy}>{busy?"Securing evidence…":"Submit anonymously"}</button></form></div>}</section><aside className="card" style={{gridColumn:"span 5",alignSelf:"start"}}><ShieldCheck size={30} color="var(--green)"/><h2>Privacy boundary</h2><p>The authenticated session is used only to authorize access and create a non-reversible daily limit token. The complaint itself cannot reference that token or your account.</p><div className="badge green">EXIF removed</div> <div className="badge green">No identity field</div></aside></div></AppShell>}
