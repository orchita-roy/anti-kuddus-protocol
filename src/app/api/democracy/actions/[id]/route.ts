import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { DecisionAction } from "@/models/DecisionAction";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{if(!await requireRole("student","captain","teacher","admin"))return fail("FORBIDDEN","Sign in required",403);await connectToDatabase();const item=await DecisionAction.findById((await params).id).select("-responsibleUserId -verifiedBy -completionEvidenceFileId").lean();return item?ok(item):fail("NOT_FOUND","Action not found",404)}catch(e){return errorResponse(e)}}
