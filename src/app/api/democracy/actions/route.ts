import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { DecisionAction } from "@/models/DecisionAction";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(){try{if(!await requireRole("student","captain","teacher","admin"))return fail("FORBIDDEN","Sign in required",403);await connectToDatabase();return ok(await DecisionAction.find().sort({createdAt:-1}).select("-responsibleUserId -verifiedBy -completionEvidenceFileId").lean())}catch(e){return errorResponse(e)}}
