import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { DemocraticPoll } from "@/models/DemocraticPoll";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(){try{if(!await requireRole("student","captain","teacher","admin"))return fail("FORBIDDEN","Sign in required",403);await connectToDatabase();return ok(await DemocraticPoll.find().sort({votingStartsAt:-1}).select("-createdBy").lean())}catch(e){return errorResponse(e)}}
