import { connectToDatabase } from "@/lib/mongodb";import { RumorCheck } from "@/models/RumorCheck";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(_:Request,{params}:{params:Promise<{publicId:string}>}){try{await connectToDatabase();const item=await RumorCheck.findOne({publicId:(await params).publicId}).select("-technicalIndicators.fingerprint").lean();return item?ok(item):fail("NOT_FOUND","TruthLens result not found",404)}catch(e){return errorResponse(e)}}
