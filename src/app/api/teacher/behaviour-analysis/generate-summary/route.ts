import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { getBehaviourAnalysis } from "@/services/behaviour-intelligence/behaviour-analysis.service";import { ok,fail,errorResponse } from "@/lib/api";export async function POST(){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();const a=await getBehaviourAnalysis();return ok({summary:`The heuristic incident risk is ${a.riskLevel} (${a.riskScore}/100) and the recent trend is ${a.trend}. ${a.hotspot?`${a.hotspot.location} is the leading reported hotspot.`:"No hotspot has sufficient evidence."}`,limitations:a.limitations,generatedBy:"deterministic"})}catch(e){return errorResponse(e)}}
