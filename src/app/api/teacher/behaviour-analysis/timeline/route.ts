import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { Complaint } from "@/models/Complaint";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();return ok(await Complaint.aggregate([{$match:{status:"verified"}},{$group:{_id:{$dateToString:{format:"%Y-%m-%d",date:"$incidentDate"}},count:{$sum:1}}},{$sort:{_id:1}}]))}catch(e){return errorResponse(e)}}
