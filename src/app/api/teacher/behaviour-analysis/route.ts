import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { getBehaviourAnalysis } from "@/services/behaviour-intelligence/behaviour-analysis.service";import { BehaviourAnalysisSchema } from "@/services/behaviour-intelligence/behaviour-summary.service";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();return ok(BehaviourAnalysisSchema.parse(await getBehaviourAnalysis()))}catch(e){return errorResponse(e)}}
