import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { SchoolRule } from "@/models/SchoolRule";import { ok,fail,errorResponse } from "@/lib/api";
export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();return ok(await SchoolRule.findByIdAndUpdate((await params).id,await r.json(),{new:true,runValidators:true}),"Rule updated")}catch(e){return errorResponse(e)}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();await SchoolRule.findByIdAndDelete((await params).id);return ok({deleted:true})}catch(e){return errorResponse(e)}}
