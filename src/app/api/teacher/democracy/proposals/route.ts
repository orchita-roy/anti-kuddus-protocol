import { requireRole } from "@/lib/permissions";import { connectToDatabase } from "@/lib/mongodb";import { Proposal } from "@/models/Proposal";import { ok,fail,errorResponse } from "@/lib/api";export async function GET(){try{if(!await requireRole("teacher","admin"))return fail("FORBIDDEN","Teacher access required",403);await connectToDatabase();return ok(await Proposal.find().select("-submissionToken").sort({createdAt:-1}).lean())}catch(e){return errorResponse(e)}}
