export type SequenceEvent={at:Date;stage:string};
export function detectRecurringSequences(events:SequenceEvent[],stages=["authority_claim","demand","conflict","complaint_or_sos"],windowMs=86400000){const ordered=[...events].sort((a,b)=>+a.at-+b.at);let occurrences=0;for(let i=0;i<ordered.length;i++){let cursor=i;const start=+ordered[i].at;if(ordered[i].stage!==stages[0])continue;for(let s=1;s<stages.length;s++){cursor=ordered.findIndex((e,j)=>j>cursor&&e.stage===stages[s]&&+e.at-start<=windowMs);if(cursor<0)break;if(s===stages.length-1)occurrences++}}return occurrences?[{steps:stages,occurrenceCount:occurrences,explanation:"Every displayed stage exists in time-ordered incident data."}]:[]}
