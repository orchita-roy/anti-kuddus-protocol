import { z } from "zod";
export const TruthLensResponseSchema=z.object({status:z.enum(["SUPPORTED","LIKELY_SUPPORTED","MISLEADING","LIKELY_FALSE","UNVERIFIED","INSUFFICIENT_EVIDENCE"]),heuristicEvidenceConfidence:z.number().min(0).max(100),claim:z.string(),explanation:z.string(),reasons:z.array(z.string()),evidence:z.array(z.object({sourceTitle:z.string(),sourceType:z.string(),section:z.string().nullable(),pageNumber:z.number().nullable(),relevanceScore:z.number()})),recommendedAction:z.string(),technicalIndicators:z.object({duplicateDetected:z.boolean(),metadataRemoved:z.boolean(),recompressionIndicator:z.enum(["none","low","moderate","high"])}).nullable()});
