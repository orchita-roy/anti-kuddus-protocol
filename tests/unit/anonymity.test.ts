import { describe,expect,it } from "vitest";import { Complaint } from "../../src/models/Complaint";import { SosAlert } from "../../src/models/SosAlert";
describe("identity separation",()=>{it("complaint schema contains no identity link",()=>{const paths=Object.keys(Complaint.schema.paths);for(const forbidden of ["submittedBy","userId","studentId","rollNumber","rollHash","email","sessionId","ipAddress"])expect(paths).not.toContain(forbidden)});it("SOS schema contains no sender identity",()=>{const paths=Object.keys(SosAlert.schema.paths);expect(paths).not.toContain("studentId");expect(paths).not.toContain("createdBy")})});
