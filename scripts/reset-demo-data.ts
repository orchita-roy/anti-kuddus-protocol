import mongoose from "mongoose";import dotenv from "dotenv";import { resetCompetitionDemo } from "../src/services/demo/competition-demo.service";dotenv.config({path:".env.local"});async function run(){if(!process.env.MONGODB_URI)throw new Error("MONGODB_URI is required");await mongoose.connect(process.env.MONGODB_URI,{dbName:process.env.MONGODB_DB_NAME||"anti_kuddus_protocol"});await resetCompetitionDemo();console.log("Only records explicitly labelled as competition demo data were cleared. Real user data was preserved.");await mongoose.disconnect()}run().catch(e=>{console.error(e);process.exit(1)});
