import mongoose from "mongoose";import dotenv from "dotenv";dotenv.config({path:".env.local"});
async function run(){if(!process.env.MONGODB_URI)throw new Error("MONGODB_URI is required");await mongoose.connect(process.env.MONGODB_URI,{dbName:process.env.MONGODB_DB_NAME||"anti_kuddus_protocol"});for(const name of ["complaints","submissionlimits","seatplans","ledgerentries","sosalerts","auditlogs","sourcedocuments","documentchunks"]){try{await mongoose.connection.collection(name).deleteMany({})}catch{}}console.log("Transactional demo data cleared. Run npm run seed to restore the guided demo.");await mongoose.disconnect()}run().catch(e=>{console.error(e);process.exit(1)});
