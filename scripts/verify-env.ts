import dotenv from "dotenv";dotenv.config({path:".env.local"});const required=["MONGODB_URI","MONGODB_DB_NAME","AUTH_SECRET","ROLL_LOOKUP_SECRET","COMPLAINT_HMAC_SECRET","VOTE_HMAC_SECRET"];const missing=required.filter(k=>!process.env[k]);if(missing.length){console.error(`Missing: ${missing.join(", ")}`);process.exit(1)}console.log("Required environment variables are present. Gemini and Pusher remain optional with documented fallbacks.");
